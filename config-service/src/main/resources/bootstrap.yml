server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wuyao1994/spring-cloud-event-sourcing-config.git
      label: dev